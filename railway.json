{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c \"python manage.py migrate_schemas --shared; python manage.py migrate_schemas --tenant; python manage.py seed_socpa_coa --all; python manage.py collectstatic --noinput; daphne -b 0.0.0.0 -p $PORT erp_system.asgi:application\"",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}